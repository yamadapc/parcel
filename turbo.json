{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "clean": {},
    "build:babel": {
      "inputs": ["src/**"],
      "outputs": ["lib/**"]
    },
    "build-ts": {
      "dependsOn": ["^build-ts"],
      "inputs": ["**/*.js"],
      "outputs": ["**/*.d.ts", "**/*.ts"]
    },
    "test:unit": {
      "inputs": ["src/**", "test/**"]
    }
  }
}
